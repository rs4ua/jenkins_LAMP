  db:
    image: registry.docker.loc/root/lamp:sql
    container_name: mysql_server
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: secret
    volumes:
      - db-data:/var/lib/mysql

  phpmyadmin:
    depends_on:
      - db
    image: registry.docker.loc/root/lamp:php
    container_name: myphp_sql
    restart: always
    environment:
      PMA_ARBITRARY: 1

volumes:
  db-data:
